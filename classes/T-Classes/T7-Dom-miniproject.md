What is DOM
What is window object
create dom struncture
### DOM methods( type)
    - selecting
    - reading and upatding
    - attributes
    - styling
    - class manupulating
    - crud elments

DOM - Document object modal
- tree representation of html
- interface access ot manipulate


## Selecting
- document.getElementById("id") // return the element with the same id - html COllection
- document.getElementsByClassName('class') // return the collection of all element by that class- html COllection
- document.getElementsByTagName('p') // return the collection of all element by that tag - html COllection
- document.querySelector('#id')// return the element with the same id - nodelist
- document.querySelectorAll('.para')// return the all element with the same class - nodelist

html COllection - Live, update automactically
nodelist (collection of dom nodes) with foreach, no live, no update

## reading and upadating elements