# Understand Frameworks

NextJS
fullstack  frmawork  => frontend backend = fullstack
lib - free from rules
frmwwork - with rules

backend = nodejs, express

Fullstack
- routing
- API's

Tier-1 => *nextjs,* nuxt, sveletekit, remix
Tier-2 => tanstack start, solid start

NextJS vs ReactJS
React => components, statemanagement, UI rendering
NextJS => Routing, react, backend, SSR, OPtimizations, SEO, Single click deployment, 


npx create nextjs@latest

File based routing
folder=pagename > page.jsx,tsx,js
folder=pagename > page.jsx,tsx,js
                >pagename > page.jsx,tsx,js
folder=pagename > page.jsx,tsx,js
                >[id] > page.jsx,tsx,js
                    async{params} the get value of id
                ...slug for array
                slug.join('/')
                catch all routes
                optional catch all routes
Route groups
(folder) bracket can ignore the folder name from url
layout.tsx can inside play the layout for inner pages

Rendering
CSR = Client Side Rendering
    => sever send emplty html + JS, browser fetch data and render UI
SSR = Server Side Rendering **TTFB**
    => compiled on the server and send to the Browser to render
SSG = Static Site Generation
    = html generated at build time, served super fast via cdn
ISR = Incremental Static regenration
    = Trigger regenrations

Link Components = use to link like ancher  a tag

Images Components = use for include image with Image tag

Font components
loading, not-found.tsx and error.tsx used as per requirement at when page.tsx need











mozart symphony
lofi beats
40hz binaural beats






