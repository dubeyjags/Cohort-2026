const num = 10;
console.log(num);

const pi = 3.14;
console.log(pi);
const fixedNum = 5.6789;
console.log(fixedNum.toFixed(2)); // Using toFixed to specify the number of decimal places

const precisionNum = 4.26789; // Using toPrecision to specify the total number of significant digits

console.log(precisionNum.toPrecision(1)); // Using toPrecision to specify the total number of significant digits
console.log(precisionNum.parseInt); // Output: false